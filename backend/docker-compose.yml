version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    init: true
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./audio_files:/app/audio_files
    restart: unless-stopped